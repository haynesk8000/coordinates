Update the application with the following enhancements.

### 1. Add a New **Velocity Equations** Section

Create a new section immediately **below the "Equations" section**.

This section should display the **symbolic equations** for the projectile's velocity components. Display the equations in symbolic form only (do not substitute numerical values).

Include the following equations:

* **Horizontal Velocity**

  * **vₓ = v₀ cos(θ)**
* **Vertical Velocity**

  * **vᵧ = v₀ sin(θ) − gt**

The equations should use the same mathematical formatting and styling as the existing equations displayed in the application.

---

### 2. Update the **Component Breakdown** Section

Modify the **Component Breakdown** table by inserting a new row immediately **below "Initial Velocity"** and **above "Acceleration."**

The new row should be labeled:

**Current Velocity**

This row should display the current velocity components calculated at the current simulation time:

* **vₓ = current horizontal velocity**
* **vᵧ = current vertical velocity**

Requirements:

* Update the displayed values continuously as the simulation runs or when the current time changes.
* Use the same formatting, alignment, units, and styling as the other rows in the Component Breakdown section.
* Ensure the values remain synchronized with the simulation state at all times.
